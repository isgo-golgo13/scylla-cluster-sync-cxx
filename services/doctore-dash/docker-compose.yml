# services/doctore-dash/docker-compose.yml
#
# Standalone demo mode for Doctore Dashboard
# Run: docker-compose up
#
# For live mode with C++ backend, use the root docker-compose.yaml instead.

version: '3.8'

services:
  doctore-dash:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: doctore-dash
    ports:
      - "3000:3000"
    environment:
      - DATA_SOURCE=mock
    restart: unless-stopped
    labels:
      - "app=doctore"
      - "component=dashboard"
